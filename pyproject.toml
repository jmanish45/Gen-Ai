[project]
name = "gen-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Manish", email = "jmanish2406@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.14",
]

[project.scripts]
gen-ai = "gen_ai:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
