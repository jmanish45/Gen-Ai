# GEN-AI

Minimal Python package scaffolded with `uv`.

## Layout

- `README.md` - project overview
- `pyproject.toml` - package metadata and dependencies
- `src/gen_ai/` - application package
- `uv.lock` - locked dependency graph

## Run

Use the package entry point with:

```bash
uv run gen-ai
```

